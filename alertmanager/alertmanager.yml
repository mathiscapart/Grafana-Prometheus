global:

route:
  receiver: pagerduty
  group_by: ['alertname', 'cluster']

  routes:
    - match:
        severity: 'warning'
      receiver: pagerduty

receivers:
  - name: pagerduty
    pagerduty_configs:
    - service_key: YOUR-INTEGRATION-KEY
